name: app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.8.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: any

  cupertino_icons: ^1.0.8
  flutter_svg: ^2.0.10+1
  image_picker: ^1.1.2

  firebase_core: ^4.4.0
  firebase_auth: ^6.1.4
  cloud_firestore: ^6.1.2
  firebase_storage: ^13.0.6
  firebase_messaging: ^16.0.0
  cloud_functions: ^6.0.0
  google_sign_in: ^6.2.2
  sign_in_with_apple: ^7.0.1
  crypto: ^3.0.3

  # State management — introduced progressively for new features only
  flutter_riverpod: ^2.6.1
  shared_preferences: ^2.3.5

  # Geo + Places (Week 1 + Week 3)
  geoflutterfire_plus: ^0.0.34
  http: ^1.2.2

  # Camera + video (Week 2 verification)
  camera: ^0.11.0
  video_player: ^2.9.2
  permission_handler: ^11.3.1

  # Privacy + secure storage (Week 1 API keys, Week 4 watermark)
  flutter_secure_storage: ^9.2.2
  image: ^4.2.0

  # UI helpers (Week 4)
  cached_network_image: ^3.4.1
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0

  # Store-prep tooling (Week 5). Run once per icon/splash design update:
  #   dart run flutter_launcher_icons
  #   dart run flutter_native_splash:create
  flutter_launcher_icons: ^0.14.1
  flutter_native_splash: ^2.4.2

# App-icon generation. The PNG paths below are placeholders that ship with
# the existing assets folder; once the creative team finalises the
# pineapple-motif icon, drop the new PNG at `assets/branding/icon.png`
# and re-run the generator. Adaptive icon background uses the brand
# burgundy.
flutter_launcher_icons:
  android: true
  ios: true
  remove_alpha_ios: true
  image_path: "assets/branding/icon.png"
  adaptive_icon_background: "#B01030"
  adaptive_icon_foreground: "assets/branding/icon_foreground.png"
  min_sdk_android: 23

# Native splash screen (burgundy gradient + Affinity wordmark from Figma).
# Background matches the gradient's bottom-left dark hex sampled from the
# Figma file so any letterbox strip blends into the artwork.
flutter_native_splash:
  color: "#580818"
  image: "assets/branding/splash.png"
  android_12:
    image: "assets/branding/splash.png"
    color: "#580818"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  generate: true

  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/apple.svg
    - assets/images/forgot_password_code.png
    - assets/images/forgot_password_graphic.svg
    - assets/images/forgot_password_image.png
    - assets/images/forgot_password_success.png
    - assets/images/google.svg
    - assets/images/group_48095449.png
    - assets/images/icon_edit_profile.png
    - assets/images/icon_favorites_couples.png
    - assets/images/icon_help.png
    - assets/images/icon_manage_trip.png
    - assets/images/icon_security.png
    - assets/images/icon_settings.png
    - assets/images/logo.svg
    - assets/images/logo_white.svg
    - assets/images/pineapple_filter_button.jpeg
    - assets/branding/README.md

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
