<?xml version="1.0" encoding="utf-8"?>
<!-- Launch background painted before Flutter / native_splash takes over.
     Burgundy so the brief flash between the system SplashScreen API frame
     and our flutter_native_splash artwork stays consistent in colour
     instead of flashing white. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/brand_primary_dark" />
</layer-list>
