class AppDimensions {
  // Private constructor to prevent instantiation
  AppDimensions._();

  // Border radius values
  static const double containerBorderRadius = 25.0;
  
  // Switch button dimensions
  static const double switchButtonRadius = 20.0;
  static const double switchButtonHeight = 50.0;
  static const double switchSelectorRadius = 20.0;
  static const double switchButtonMargin = 4.0;
  
  // System margins
  static const double systemMargin = 20.0;
}
